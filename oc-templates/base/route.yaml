---
kind: Route
apiVersion: route.openshift.io/v1
metadata:
  name: "demo-mlflow-agent-tracing-route"
  labels:
    shard: internal
spec:
  port:
    targetPort: 8000
  to:
    kind: Service
    name: "demo-mlflow-agent-tracing-service"
    weight: 100
  tls:
    termination: edge
  wildcardPolicy: None
